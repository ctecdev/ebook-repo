'use strict';

/**
 * @ngdoc function
 * @name MyApp.controller:MainCtrl
 * @description
 * # MainCtrl
 * Controller of the MyApp
 */

var MainController = function($scope, Restangular, $uibModal, $log, _){
	
}

MainController.$inject = ['$scope', 'Restangular', '$uibModal', '$log', '_'];
MyApp.controller('MainController', MainController);